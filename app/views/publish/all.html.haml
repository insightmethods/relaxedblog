%h2 Blog pages
%h3
  = link_to 'New page', url(:publish, :action => "new")
%br
%ul#page-list
- for page in @pages
  %li
    %span= link_to page.title, url(:publish, :action => "edit", :id => page.id)
    %span= link_to 'delete', url(:publish, :action => "destroy", :id => page.id)